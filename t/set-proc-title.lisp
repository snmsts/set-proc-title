(in-package :cl-user)
(defpackage set-proc-title-test
  (:use :cl
        :set-proc-title
        :prove))
(in-package :set-proc-title-test)

;; NOTE: To run this test file, execute `(asdf:test-system :set-proc-title)' in your Lisp.

(plan nil)

;; blah blah blah.

(finalize)
